# Быстрый старт: Деплой в облако

## Самый простой способ: Railway ⭐

### Шаг 1: Подготовка
1. Убедитесь, что ваш код в GitHub репозитории
2. Локально запустите один раз `scripts/run_daily.py`, чтобы создать файл `anon_news.session`

### Шаг 2: Деплой на Railway
1. Зайдите на [railway.app](https://railway.app) и войдите через GitHub
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит Python проект и запустит `Procfile`

### Шаг 3: Настройка переменных окружения
В настройках проекта (Settings → Variables) добавьте:

```
API_ID=YOUR_API_ID
API_HASH=YOUR_API_HASH
TELEGRAM_BOT_TOKEN=ваш_токен_бота
OPENAI_API_KEY=ваш_ключ_openai
FOLDER_NAME=GPT
DEBUG_MODE=False
```

### Шаг 4: Загрузка файла сессии
1. Скопируйте `anon_news.session` с локального компьютера
2. В Railway: Settings → Variables → Add Variable
   - Name: `TELEGRAM_SESSION` (или загрузите через Railway CLI)
3. Или используйте Railway CLI для загрузки файла:
   ```bash
   railway login
   railway link
   railway variables set TELEGRAM_SESSION="$(base64 -i anon_news.session)"
   ```

### Шаг 5: Настройка ежедневной рассылки
1. В Railway: New → Scheduled Task
2. Cron: `0 9 * * *` (каждый день в 09:00 UTC)
3. Command: `python scripts/run_daily.py --send`

### Готово! 🎉
Бот теперь работает 24/7 и обрабатывает команды пользователей.

---

## Альтернатива: Render

1. Зайдите на [render.com](https://render.com)
2. New → Background Worker
3. Подключите GitHub репозиторий
4. Build Command: `pip install -r requirements.txt`
5. Start Command: `python scripts/get_users.py`
6. Добавьте переменные окружения (как в Railway)
7. Для ежедневной рассылки создайте отдельный Worker или используйте Cron Job

---

## Важно!

⚠️ **Файл сессии Telethon** (`anon_news.session`) необходим для работы. Его нужно:
- Создать локально (запустите `scripts/run_daily.py` один раз)
- Загрузить на сервер (через Railway CLI, SSH или как переменную окружения)

⚠️ **Безопасность**: Не коммитьте `config.py` с реальными ключами в публичный репозиторий!

---

## Проверка работы

После деплоя проверьте:
1. Логи в Railway/Render - должны быть сообщения "Бот запущен, ожидает сообщений..."
2. Отправьте боту `/start` в Telegram
3. Проверьте, что бот отвечает

---

Подробная инструкция: см. `DEPLOY.md`
