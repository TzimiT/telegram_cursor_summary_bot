# Чеклист для деплоя бота в облако

## ✅ Подготовка перед деплоем

- [ ] Код загружен в GitHub репозиторий
- [ ] Локально создан файл `anon_news.session` (запустите `scripts/run_daily.py` один раз)
- [ ] Все зависимости указаны в `requirements.txt`
- [ ] `config.py` обновлен для поддержки переменных окружения

## 🚀 Деплой на Railway (рекомендуется)

- [ ] Зарегистрировались на Railway.app
- [ ] Создали новый проект из GitHub репозитория
- [ ] Добавили переменные окружения:
  - [ ] `API_ID`
  - [ ] `API_HASH`
  - [ ] `TELEGRAM_BOT_TOKEN`
  - [ ] `OPENAI_API_KEY`
  - [ ] `FOLDER_NAME`
  - [ ] `DEBUG_MODE=False`
- [ ] Загрузили файл `anon_news.session` (через CLI или как переменную)
- [ ] Настроили Scheduled Task для ежедневной рассылки (cron: `0 9 * * *`, команда: `python scripts/run_daily.py --send`)
- [ ] Проверили логи - бот должен быть запущен
- [ ] Протестировали бота командой `/start` в Telegram

## 🔍 Проверка работы

- [ ] Бот отвечает на команды (`/start`, `/help`, `/status`)
- [ ] Логи показывают "Бот запущен, ожидает сообщений..."
- [ ] Ежедневная рассылка настроена и работает
- [ ] Нет ошибок в логах

## 📝 Полезные команды

### Локально (для подготовки)
```bash
# Создать файл сессии
python scripts/run_daily.py --channels

# Закодировать сессию для загрузки в облако
python scripts/upload_session.py
```

### В облаке (Railway CLI)
```bash
# Подключиться к проекту
railway login
railway link

# Посмотреть логи
railway logs

# Добавить переменную окружения
railway variables set API_ID=YOUR_API_ID
```

## ⚠️ Важные моменты

1. **Файл сессии** - критически важен для работы Telethon. Без него бот не сможет читать каналы.

2. **Переменные окружения** - используйте их вместо хардкода в `config.py` для безопасности.

3. **Логи** - регулярно проверяйте логи на наличие ошибок.

4. **Мониторинг** - настройте уведомления о падении бота (Railway имеет встроенный мониторинг).

5. **Бэкапы** - регулярно делайте бэкапы `subscribers.json` и других важных файлов.

## 🆘 Решение проблем

### Бот не отвечает
- Проверьте логи в Railway
- Убедитесь, что переменные окружения установлены правильно
- Проверьте токен бота

### Ошибки Telethon
- Убедитесь, что файл сессии загружен
- Проверьте `API_ID` и `API_HASH`

### Ежедневная рассылка не работает
- Проверьте настройки Scheduled Task
- Убедитесь, что команда правильная: `python scripts/run_daily.py --send`
- Проверьте логи выполнения задачи

---

**Готово!** Ваш бот теперь работает в облаке 24/7 🎉
